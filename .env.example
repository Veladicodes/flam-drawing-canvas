# PartyKit server host — hostname only, no protocol, no trailing slash.
# Local dev (default when unset): 127.0.0.1:1999
# Production: the host printed by `npx partykit deploy`,
#   e.g. flam-drawing-canvas.<your-partykit-username>.partykit.dev
VITE_PARTYKIT_HOST=127.0.0.1:1999
